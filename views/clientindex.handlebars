<html>
<nav>
  <ul class="nav nav-pills pull-right">
      <li role="presentation"><a href="/users/clientagreement">Control Agreement</a></li>
  </ul>
</nav>
    <h2 class="page-header">Client Dashboard</h2>
    {{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
    {{/if}}
  <body>
    <h1></h1>
    <!-- Wrapper-->
        <h3>Loan Information</h3>
      <div id="accountList">
        <table style = "width: 100%" >
        <style>
          table, th, td {
              border: 1px solid black;
              border-collapse: collapse;
          }
          th, td {
              padding: 5px;
              text-align: left;
          }
          </style>
          <tr>
            <th>Broker</th>
            <th>Lending Bank</th>
            <th>Loan Amount</th>
            <th>Total Collateral</th>
            <th>Loan Credit</th>
            <th>Status</th>
          </thead>
          <tbody></tbody>
        </table>
      </div>
    <!-- /WRAPPER-->
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
    <script src="/javascripts/global.js"></script>
  </body>
</html>